spring.application.name=FiscalFlowSpring

spring.datasource.url=jdbc:mysql://localhost:3306/fiscalflow

# 2. CREDENCIAIS (Usuário e Senha do seu MySQL)
spring.datasource.username=root
spring.datasource.password=mysqlPW

# 3. CONFIGURAÇÕES DO JPA / HIBERNATE (A Mágica)
# O 'update' faz o Spring criar ou atualizar as tabelas automaticamente baseado no seu @Entity
spring.jpa.hibernate.ddl-auto=update

# Mostra o SQL no console (bom para ver o Spring trabalhando)
spring.jpa.show-sql=true
